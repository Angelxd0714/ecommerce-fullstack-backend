name: Deploy to EC2

on:
  push:
    branches: [master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Log in to Amazon ECR
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build and push Docker image
      run: |
        docker build -t $ECR_REPO:latest .
        docker push $ECR_REPO:latest
      env:
        ECR_REPO: ${{ secrets.ECR_REPO }}

    - name: Create PEM file
      run: |
        echo "${{ secrets.EC2_KEY }}" > key.pem
        chmod 600 key.pem

    - name: SSH into EC2 and deploy container
      run: |
        ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << EOF
          # Iniciar sesión en ECR
          aws ecr get-login-password --region $AWS_REGION | sudo docker login --username AWS --password-stdin $ECR_REGISTRY

          # Guardar archivo de entorno
          echo "${ENV_PRODUCTION}" > /home/ec2-user/.env.temp

          # Eliminar contenedor antiguo si existe
          sudo docker stop app || true
          sudo docker rm app || true

          # Pull de imagen desde ECR
          sudo docker pull $ECR_REPO:latest

          # Ejecutar nuevo contenedor
          sudo docker run -d --name app \
            --env-file /home/ec2-user/.env.temp \
            -p 3000:3000 \
            $ECR_REPO:latest

          # Eliminar archivo temporal
          rm /home/ec2-user/.env.temp
        EOF
      env:
        ECR_REPO: ${{ secrets.ECR_REPO }}         # Ej: 123456789012.dkr.ecr.us-east-1.amazonaws.com/ecommerce-backend
        ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }} # Ej: 123456789012.dkr.ecr.us-east-1.amazonaws.com
        ENV_PRODUCTION: ${{ secrets.ENV_PRODUCTION }}
        AWS_REGION: ${{ secrets.AWS_REGION }}
        EC2_USER: ${{ secrets.EC2_USER }}
        EC2_HOST: ${{ secrets.EC2_HOST }}
        EC2_KEY: ${{ secrets.EC2_KEY }}
